apiVersion: v1
kind: Service
metadata:
  name: boardgame-api
  namespace: boardgame
  labels: { app: boardgame-api }
spec:
  selector: { app: boardgame-api }
  ports: [{ name: http, port: 80, targetPort: http }]
  type: ClusterIP
